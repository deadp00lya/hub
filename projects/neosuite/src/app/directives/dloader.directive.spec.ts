import { DCLoaderDirective } from './dloader.directive';

describe('DLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DCLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
